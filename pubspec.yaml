name: mobile_listing_app
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  stacked: ^3.4.1
  stacked_services: 
  dio: 
  retrofit: 
  carousel_slider: 
  shimmer: 
  flutter_svg: 
  intl: 
  pull_to_refresh:
  loading_overlay: 
  cached_network_image: ^3.2.3
  firebase_core: ^2.14.0
  firebase_messaging: ^14.6.4
  flutter_local_notifications: ^15.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  retrofit_generator: 
  build_runner: 



flutter:
  uses-material-design: true
  assets:
    - assets/
   
